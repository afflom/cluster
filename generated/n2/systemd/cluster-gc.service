# @generated by `just render` from model/. Do not edit.
# R1: the model is the single source. A hand-edit here is the same class
# of error as a hand-edited CONFORMANCE.md, and check-render says so.
# Asserted by: CD-08, CD-09

# Retention (§5.5). Container images older than 168h, and on the registry
# node an untagged manifest older than 30 days.

[Unit]
Description=Reclaim container and registry storage

[Service]
Type=oneshot
ExecStart=/usr/bin/podman system prune --force --filter until=168h
ExecStart=/usr/bin/ostree admin prune --retain-only=2
Environment=CLUSTER_MEASUREMENT_OUTPUT_PRUNED=false

