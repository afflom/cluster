# @generated by `just render` from model/. Do not edit.
# R1: the model is the single source. A hand-edit here is the same class
# of error as a hand-edited CONFORMANCE.md, and check-render says so.
# Asserted by: CD-03, CD-14, CD-09

# n2 loads its ruleset from the image rather than from /etc (§4.4).
#
# The rendered file is diff-gated under generated/; reading it where it was
# shipped means the running ruleset and the reviewed one cannot differ by
# anything that happened on the node.

[Service]
ExecStart=
ExecStart=/usr/sbin/nft -f /usr/lib/cluster/nftables.conf

