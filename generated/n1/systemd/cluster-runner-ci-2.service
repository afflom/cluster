# @generated by `just render` from model/. Do not edit.
# R1: the model is the single source. A hand-edit here is the same class
# of error as a hand-edited CONFORMANCE.md, and check-render says so.
# Asserted by: CD-05, CD-09, CD-16

# Actions runner ci-2 on n1. --ephemeral: the runner exits after one
# job, so draining this node is a matter of not re-registering rather
# than of killing work in flight (§14.1).

[Unit]
Description=Actions runner ci-2
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
Environment=RUNNER_NAME=ci-2
Environment=RUNNER_LABELS=self-hosted,linux,x64,cluster,ci
Environment=RUNNER_EPHEMERAL=true
EnvironmentFile=/etc/cluster/runner.env
ExecStart=/usr/libexec/cluster/runner-loop
Restart=always
RestartSec=15

[Install]
WantedBy=multi-user.target

