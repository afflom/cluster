# @generated by `just render` from model/. Do not edit.
# R1: the model is the single source. A hand-edit here is the same class
# of error as a hand-edited CONFORMANCE.md, and check-render says so.
# Asserted by: CD-12, CS-04, CD-09

# The registry on n1. It hosts this repository's images, mirrors them from
# GHCR every five minutes, and pull-through caches the declared upstreams
# so a node's pull does not leave the mesh when it does not have to (§5.4).
#
# Bound to the mesh loopback: the registry is a mesh service and the
# management plane's firewall does not open its port (§4.4).
{
  "distSpecVersion": "1.1.0",
  "storage": { "rootDirectory": "/var/lib/registry", "gc": true,
    "gcDelay": "720h", "gcInterval": "24h" },
  "http": { "address": "10.10.255.1", "port": "5000" },
  "log": { "level": "info" },
  "extensions": {
    "sync": {
      "enable": true,
      "registries": [
        {
          "urls": ["https://ghcr.io"],
          "onDemand": false,
          "pollInterval": "5m",
          "content": [{ "prefix": "/afflom/cluster/**" }]
        },
        {
          "urls": ["https://ghcr.io"],
          "onDemand": true,
          "tlsVerify": true
        },
        {
          "urls": ["https://docker.io"],
          "onDemand": true,
          "tlsVerify": true
        }
      ]
    }
  }
}
