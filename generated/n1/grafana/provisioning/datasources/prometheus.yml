# @generated by `just render` from model/. Do not edit.
# R1: the model is the single source. A hand-edit here is the same class
# of error as a hand-edited CONFORMANCE.md, and check-render says so.
# Asserted by: CD-12, CD-09, CD-16

# Grafana's datasource on n1. Provisioned rather than clicked: a Grafana
# with no datasource starts, reports itself active, and shows nothing ---
# which §10.1's fifth check cannot tell from working (§18).

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://10.10.255.1:9090
    isDefault: true
    jsonData: { timeInterval: "15s", timeSeriesRetention: "30d" }
