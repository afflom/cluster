# @generated by `just render` from model/. Do not edit.
# R1: the model is the single source. A hand-edit here is the same class
# of error as a hand-edited CONFORMANCE.md, and check-render says so.
# Asserted by: CD-03, CD-18, CD-09, CD-16

# What the `storage` role adds to the input chain (§4.4, §8.4).
#
# Copied to /run/cluster/nftables-role.conf by cluster-init once the role is
# known, and included from nftables.conf. Rendered even when it is empty:
# nft fails to load a ruleset that includes a file which is not there, so
# `no rules` has to be a file that says so.

# cluster-ctl, published by tailscale serve
iifname "tailscale0" tcp dport 443 accept
