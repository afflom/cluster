# @generated by `just render` from model/. Do not edit.
# R1: the model is the single source. A hand-edit here is the same class
# of error as a hand-edited CONFORMANCE.md, and check-render says so.
# Asserted by: CD-14, CB-03, CD-09, CD-16

# SELinux: enforcing, targeted, and it stays that way (§8.3).
#
# The custom module is compiled at build time and shipped in
# /usr/share/selinux/. Nothing compiles policy at runtime: root is
# read-only, and a policy build on a running node would be a write to a
# filesystem that does not take writes.

SELINUX=enforcing
SELINUXTYPE=targeted
