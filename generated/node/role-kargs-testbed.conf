# @generated by `just render` from model/. Do not edit.
# R1: the model is the single source. A hand-edit here is the same class
# of error as a hand-edited CONFORMANCE.md, and check-render says so.
# Asserted by: CD-06, CD-19, CD-09, CD-16

# The kernel arguments a machine holding the `testbed` role applies once it
# knows it holds it (§8.5).
#
# Applied with `bootc loader-entries set-options-for-source --source
# cluster-role`, which tracks them as their own source in the BLS entry
# and re-merges them on every upgrade. A node that stops holding this
# role sets the same source empty and drops them.
#
# CPUs 2,3 are isolated, SMT is off, and C-states are constrained. Each of
# those is a constructible fact and CB- carries it. That the result yields
# stable measurements is not, and §21.1 records why.

options=isolcpus=2,3 nohz_full=2,3 rcu_nocbs=2,3 nosmt intel_idle.max_cstate=1 processor.max_cstate=1
