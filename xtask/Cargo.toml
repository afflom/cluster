[package]
name = "xtask"
description = "Repository gates: check-model, check-render, audit-limits, audit-deferral, validate."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
repo-model = { workspace = true }
cluster-model = { workspace = true }
# `installer-config` writes the configuration bootc-image-builder reads, and
# proves the kickstart survives the round trip rather than merely that the file
# parses. Both halves need a TOML reader.
toml = { workspace = true }

[lints]
workspace = true
