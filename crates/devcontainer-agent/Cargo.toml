[package]
name = "devcontainer-agent"
description = "The node-local agent: dirty computation, attachment recording, and the migration steps (SPEC.md §14.3, §15.1, §15.2)."
# Shipped: this binary is in the compute node's image, and R5 therefore binds.
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
cluster-ctl = { version = "0.1.0", path = "../cluster-ctl", default-features = false }
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
