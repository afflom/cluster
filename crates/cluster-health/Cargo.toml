[package]
name = "cluster-health"
description = "The health predicate every tier, greenboot, and the rollout ordering all consume (SPEC.md §10.1)."
# Shipped: this binary is in the base image on every node, and R5 therefore
# binds. Every error a caller can see is one model/ids.toml sanctions.
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
